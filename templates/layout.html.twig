{% extends 'base.html.twig' %}

{% block body %}
<nav class="navbar navbar-expand m-auto navbar-light bg-light fixed-top text-center">
  <a class="navbar-brand" href="{{path("index")}}">Accueil</a>
  <div class="collapse navbar-collapse" id="navbarNav">
    <ul class="navbar-nav">
      {% if is_granted('IS_AUTHENTICATED_FULLY') %}
      <li class="nav-item">
        <a class="navbar-brand" href="{{path("profil.id")}}">Mon compte</a>
      </li> 
      <li class="nav-item">
        <a class="navbar-brand" href="{{path("app_logout")}}">Déconnexion</a>
      </li> 
      {% else  %}
      <li class="nav-item">
        <a class="navbar-brand" href="{{path("app_register")}}">S'inscrire</a>
      </li> 
      <li class="nav-item">
        <a class="navbar-brand" href="{{path("app_login")}}">Se connecter</a>
      </li>
      {% endif %}
    </ul>
  </div>
</nav>


<div class="jumbotron jumbotron-fluid"><div class="container"><h1 class="display-4">Movie Rating</h1>
<p class="lead">Le premier site français pour évaluer les films les plus pires produits par le cinéma français</p>
</div>
</div>
<div class="container-fluid">
<h2>{% block pageTitle %}{% endblock %}</h2>
{% block main %}{% endblock %}
</div>
<div class="jumbotron jumbotron-fluid">
<div class="container">
<p class="lead">Si vous êtes un réalisateur français et que vous vous plaigniez des notes attribuées à votre navet, il ne fallait pas produire une telle bouze. XOXO</p>
</div>
</div>
{% endblock %}
